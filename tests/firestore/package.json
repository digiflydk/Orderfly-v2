{
  "name": "orderfly-platform-rules-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --project demo-orderfly-platform --only firestore --config firebase.json \"node --test platform-rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "firebase": "12.2.1",
    "firebase-tools": "15.30.0"
  }
}
