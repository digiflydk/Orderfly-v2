# API map

This document describes the most important internal API endpoints.

Example categories:

- Public customer API (webshop)
- Restaurant admin API
- Superadmin API

For the exact list and methods, see the JSON response from `/api/superadmin/docs/api-map`.
