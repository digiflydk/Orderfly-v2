'use server';

// src/app/admin/menu/actions.ts
// Legacy admin menu import actions disabled.
// This placeholder exists only to keep the module path valid without
// pulling in AI flows or legacy types.

export async function uploadMenuImageLegacy(): Promise<never> {
  throw new Error('Legacy admin menu import is disabled in this build.');
}
