// src/app/admin/analytics/page.tsx
// Legacy admin analytics page disabled.
// This route is kept as a no-op placeholder to avoid breaking existing URLs,
// but it no longer imports or depends on funnel types or analytics logic.

import React from "react";

export default function AdminAnalyticsPage() {
  return null;
}
