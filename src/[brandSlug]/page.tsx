// src/[brandSlug]/page.tsx
// Legacy placeholder. The real implementation lives in src/app/[brandSlug]/page.tsx.
// This file is intentionally minimal so it does not interfere with the App Router build.

import React from "react";

export default function Page() {
  return null;
}
