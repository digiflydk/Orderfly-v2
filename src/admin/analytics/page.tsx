// src/admin/analytics/page.tsx
// Legacy placeholder. The real analytics implementation now lives under
// the App Router (superadmin). This file is intentionally minimal so it
// does not interfere with the build.

import React from "react";

export default function AdminAnalyticsPage() {
  return null;
}
